class RadioSelectionModel {
  String text, key;
  bool isChecked;
  int id;

  RadioSelectionModel({
    required this.text,
    required this.key,
    required this.isChecked,
    required this.id,
  });
}
